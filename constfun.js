class Car {
    constructor(name, mileage, max_speed) {
        this.name = name;
        this.mileage = mileage;
        this.max_speed = max_speed;
    }
}

let maruthi = new Car("swift", 20, 120);
console.log(maruthi);